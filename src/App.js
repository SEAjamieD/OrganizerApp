import React, { Component } from 'react';
import { View } from 'react-native';
import OrganizerAppContainer from './components/OrganizerAppContainer';

export default class App extends Component<{}> {
  render() {
    return (
        <OrganizerAppContainer />
    );
  }
}
